// Tool createPaymentLink (write). Params: amount (optional), memo (optional).
// Creates a USDC payment or collect link or QR.
// Vpay-dependent: calls agent/mocks/vpay.ts until the app feature exists.
export {};

// Tool getBalance (read). Params: none.
// Returns the user native USDC balance via eth_getBalance and formatEther (18 decimals).
// Self-contained. TODO: implement with the publicClient from chain.ts.
export {};

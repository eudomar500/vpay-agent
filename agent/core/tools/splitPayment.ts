// Tool splitPayment (write). Params: total or each, recipients (array).
// Splits a payment across addresses (batch). Ideally one signature via AA.
// Self-contained. Pauses for confirmation.
export {};
